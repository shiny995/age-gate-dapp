pragma language_version >= 0.13.0;

import CompactStandardLibrary;

witness age(): Uint<8>;

export circuit checkEligibility(): Boolean {
    const MIN_AGE: Uint<8> = 18;
    return disclose(age() >= MIN_AGE);
}
